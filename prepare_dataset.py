import pandas as pd
import os
import argparse


def clean_sentence(text):
    return text


def load_data(data_path):
    data = None

    return data


if __name__ == '__main__':

    data = load_data('data.csv')  # columns = ['context', 'response']

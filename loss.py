import torch
import torch.nn as nn


def cosine_distance(embeddings):
    pass


def triplet_loss(embeddings):
    pass


def sample(embeddings, strategy='all'):
    # all, hard, weighted
    anchor, positive, negative = None, None, None

    return anchor, positive, negative
